import React, { useState, useRef } from 'react'
import { Text, View, StyleSheet, LayoutAnimation, TouchableOpacity, Platform, UIManager } from 'react-native'

export default function Login() {
  return (
    <View>
      <Text>login</Text>
    </View>
  )
}
